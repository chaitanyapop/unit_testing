function Handeling_prop(props)
{
    return(
        <div>
            <p>Here we are handeling props</p>
            <p>the prop data is : {props.data}</p>
        </div>
    )
}
export default Handeling_prop